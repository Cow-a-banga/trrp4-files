namespace FileSystemWork
{
    public enum MsgType
    {
        CreateDisk = 1,
        CreateDirectory,
        RenameDirectory,
        CreateFile,
        ChangeFile,
        RenameFile,
        Delete,
        LoadDisk, 
        GetSharedDirById
    }
}